#include <cstdlib>
#include <iostream>

using namespace std;

int main(int argc, char *argv[])
{
    // mantýksal operatorler
    /*
    
       <,>,<=,>=,== ifadeleri mantýksal operatorlerdir.
    
    && => ifade mantýksal ifade olarak ve'dýr.
    || => ise mantýksal olarak veya'dýr.
    mantýksal operatorleri iki veya daha fazla ifadede kullanancagýz.
    < ifadesi matemetiksel küçüktür operatörüdür.
> ifadesi ifadesi büyüktür operatorüdür.
>= büyük eþittir operatörüdür.
<= küçük eþittir operatorüdür.

== ise eþittir operatörüdür.
kullanýmý:

a<b
c>b
c<=k
k==m 
gibi


mantýksal ifadelerden && ve || veya kullanýmý
bu matematiksel olarak p ve q -  p veya q anlamýnda kullanýlýr.
(a>=b)||(c<d)  -> a büyük eþit b veya c küçük d
(a==d)&& (d<=) -> a eþit d ve d küçük eþit 5 

þeklinde kullanýlýr.

    
    */
    system("PAUSE");
    return EXIT_SUCCESS;
}
